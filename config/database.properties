# Database connection properties for UpNext App
# Customize these settings to match your MySQL database configuration

# Database host address
db.host=127.0.0.1

# Database port
db.port=3306

# Database name
db.name=upnex

# Database user credentials
db.user=root
db.password=hari

# Connection pool settings
db.pool.min=5
db.pool.max=10
db.pool.timeout=30000